data:
  dataset_url: "https://github.com/logpai/loghub/blob/master/HDFS/HDFS_2k.log"
  raw_log_path: "data/raw_logs/HDFS.log"

model:
  window_size: 10
  hidden_size: 64
  latent_size: 16
  learning_rate: 0.001
  epochs: 5
  batch_size: 128

threshold:
  percentile: 95